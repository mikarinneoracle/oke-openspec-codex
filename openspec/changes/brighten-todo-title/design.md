# Design: Brighter Todo title

The dark theme defines `primary.light` as `#7dd3fc`. Applying that existing
semantic theme token to the `Typography` page heading improves contrast and
keeps the title aligned with the UI's primary color system.
