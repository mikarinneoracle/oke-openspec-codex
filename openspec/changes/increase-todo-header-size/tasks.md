# Tasks: Larger Todo heading

- [x] Increase the `To-do list` visual heading variant from `h5` to `h4`.
- [x] Validate the UI lint and production build.
- [ ] Commit and push the reviewed source change to `main` after explicit approval.
- [ ] Promote the immutable UI release through the separate reviewed GitOps change, if deployment is requested.
