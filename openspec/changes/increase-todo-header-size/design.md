# Design: Larger Todo heading

The UI already uses a single Material UI `Typography` component with
`component="h1"` for the `To-do list` header. Changing its visual `variant`
from `h5` to `h4` increases its size while preserving the page's heading
semantics and the surrounding responsive layout.
