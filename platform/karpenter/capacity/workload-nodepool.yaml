apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: todo-workload
spec:
  template:
    metadata:
      labels:
        workload.oke-openspec-codex.io/tier: application
    spec:
      expireAfter: Never
      nodeClassRef:
        group: oci.oraclecloud.com
        kind: OCINodeClass
        name: todo-workload
      requirements:
        - key: karpenter.sh/capacity-type
          operator: In
          values:
            - on-demand
        - key: oci.oraclecloud.com/instance-shape
          operator: In
          values:
            - VM.Standard.E5.Flex
      terminationGracePeriod: 2h
  disruption:
    budgets:
      - nodes: 10%
    consolidateAfter: 10m
    consolidationPolicy: WhenEmpty
  limits:
    # OCI reports a 1-OCPU E5 node as two Kubernetes vCPUs. Keep the CPU
    # ceiling aligned with four 1-OCPU nodes and the 32-Gi memory ceiling.
    cpu: "8"
    memory: 32Gi
